### FULL COMMAND:
# `pip download -r <addon_root>/requirements.txt  --dest <addon_root>/wheels/ --only-binary=:all: --python-version=<blender_python_version>`
# refer to `<repo_root>/scripts/build.py` for more info

--extra-index-url https://download.pytorch.org/whl/cu130
--extra-index-url https://download.pytorch.org/whl/cu128
--extra-index-url https://download.pytorch.org/whl/torchcodec/

Pillow
huggingface_hub
einops
safetensors
plyfile

# for vggt build deps
setuptools
wheel
packaging

# developer packages
python-json-logger
coloredlogs
python-dotenv
psutil
dltype

# `gsplat` is installed separately within `build.py`
# git+https://github.com/nerfstudio-project/gsplat.git

# `gsplat` dependencies
jaxtyping
rich
# `ninja` should be installed globally for pytorch
# ninja

torch; sys_platform == "darwin" # get whatever poor macos users are offered
torchvision; sys_platform == "darwin"
torchcodec; sys_platform == "darwin"

torch==2.8.0+cu128; sys_platform == "win32"  # current `WinError 1114` in latest `2.9.1` branch (01-09-2026)
torchvision==0.23.0+cu128; sys_platform == "win32"
torchcodec==0.7.0; sys_platform == "win32"

torch==2.10.0+cu130; sys_platform == "linux"
torchvision==0.25+cu130; sys_platform == "linux"
torchcodec==0.10.0+cu130; sys_platform == "linux"