numpy
Pillow
huggingface_hub
einops
safetensors
plyfile

# developer packages
python-json-logger
coloredlogs

torch==2.8.0; sys_platform == "win32"  # current `WinError 1114` in latest `2.9.1` branch
torchvision==0.23.0; sys_platform == "win32"

torch; sys_platform == "darwin" # get whatever poor macos users are offered
torchvision; sys_platform == "darwin"

# use cpu versions solely for smaller downloaded wheels in linux releases
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.9.1+cpu; sys_platform == "linux"
torchvision==0.24.1+cpu; sys_platform == "linux"